name: Odds Tracker Bot

on:
  schedule:
    # Τρέχει αυτόματα κάθε μέρα στις 08:00 π.μ. (UTC)
    - cron: '0 8 * * *'
  workflow_dispatch: # Επιτρέπει και τη χειροκίνητη εκτέλεση οποιαδήποτε στιγμή

jobs:
  run-bot:
    runs-runs-on: ubuntu-latest
    steps:
      - name: Check out repo
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests gspread oauth2client pandas

      - name: Run Odds Tracker
        env:
          GOOGLE_SHEET_URL: ${{ secrets.GOOGLE_SHEET_URL }}
        run: python main.py
